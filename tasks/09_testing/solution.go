// Блок 09. Тесты, бенчмарки, профилирование.
//
// КОРОТКО: мок-фреймворки не нужны — интерфейсы реализуются неявно,
// фейк это просто ещё одна структура.
//
// ИНСАЙТ: строки неизменяемы. s += x в цикле каждый раз аллоцирует НОВУЮ строку
// и копирует всё старое: 10000 итераций = 10000 аллокаций и квадратичное
// копирование. strings.Builder пишет в растущий буфер и отдаёт строку без
// копирования. В PHP строки мутабельны, там этой проблемы нет.
//
// ИНСАЙТ: b.N подбирается автоматически — бенчмарк перезапускается с растущим N,
// пока не наберётся значимое время. Поэтому цикл до b.N обязателен, а тяжёлую
// подготовку выносят и зовут b.ResetTimer().
//
// СОБЕС:
//   - Что такое табличные тесты и почему это стандарт в Go?
//   - Как мокать зависимости без библиотек?
//   - t.Error vs t.Fatal? Что делает t.Parallel и какие с ним ловушки?
//   - Как замерить аллокации в бенчмарке? (-benchmem)
//   - Escape analysis: когда переменная уезжает в кучу? Кто это решает?
//   - Как устроен GC? (трёхцветный конкурентный mark-and-sweep)
package testingblock

// ЗАДАНИЯ (тут почти всё пишется в _test.go, не здесь):
//
//  1. Табличные тесты для BinarySearch из algorithms/. Слайс структур
//     {name, input, want} + t.Run(tt.name, ...) для подтестов. Это стандарт де-факто.
//  2. Фейковая реализация Storage из блока 03 без единой библиотеки моков.
//  3. TestMain с подготовкой и очисткой.
//  4. Бенчмарк: конкатенация через += против strings.Builder, с -benchmem.
//     Разница будет в сотни раз — пойми, откуда она берётся.
//  5. go test -cpuprofile + go tool pprof, найди горячее место.

// ConcatNaive — конкатенация через +=. Заведомо медленная, для бенчмарка.
func ConcatNaive(parts []string) string {
	panic("TODO")
}

// ConcatBuilder — то же через strings.Builder.
func ConcatBuilder(parts []string) string {
	panic("TODO")
}
