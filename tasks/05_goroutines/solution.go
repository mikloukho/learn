// Блок 05. Горутины и каналы.
//
// КОРОТКО: программа завершается вместе с main — горутины умрут молча.
// "Don't communicate by sharing memory; share memory by communicating."
//
// ИНСАЙТ: небуферизованный канал — это не очередь на 0 элементов, а РАНДЕВУ.
// Отправка блокируется, пока кто-то не начнёт читать.
//
// ИНСАЙТ: горутина — не поток ОС. Планировщик (G-M-P) мультиплексирует тысячи
// горутин на несколько системных потоков, стек растёт динамически с 2 КБ,
// переключение стоит наносекунды. До Go 1.14 пустой for{} мог намертво занять
// поток — планировщик был кооперативным.
//
// ПРАВИЛА КАНАЛОВ:
//   - Закрывает ВСЕГДА отправитель, никогда получатель.
//   - Запись в закрытый — паника. Чтение — мгновенно нулевое значение.
//   - v, ok := <-ch; ok == false значит "закрыт и пуст".
//   - range ch читает, пока не закроют. Не закроешь — дедлок.
//   - nil-канал блокирует навсегда (полезно: выключить ветку select).
//
// СОБЕС:
//   - Чем горутина отличается от потока ОС? Сколько занимает стек?
//   - Расскажи про планировщик: G, M, P. Что такое work stealing?
//   - Кто должен закрывать канал и почему?
//   - Что делает select с default? А без веток вообще?
//   - Как остановить горутину? (только попросить её саму выйти)
//   - Что такое утечка горутины и как её найти?
package goroutines

// WaitGroupDemo — запусти n горутин и дождись всех.
// wg.Add ДО запуска, defer wg.Done() первой строкой внутри.
func WaitGroupDemo(n int) {
	panic("TODO")
}

// Generator — вернуть канал ТОЛЬКО ДЛЯ ЧТЕНИЯ (<-chan), писать в него
// из горутины, закрыть по окончании. Кто создал — тот и закрывает.
func Generator(n int) <-chan int {
	panic("TODO")
}

// WorkerPool — workers горутин читают из jobs, пишут в возвращаемый канал.
// Классическая задача с собеса, умей писать её с закрытыми глазами.
func WorkerPool(jobs <-chan int, workers int) <-chan int {
	panic("TODO")
}

// ReadWithTimeout — select с time.After. Второе значение false, если не дождались.
func ReadWithTimeout(ch <-chan int, ms int) (int, bool) {
	panic("TODO")
}

// FanIn — слить два канала в один. Закрыть результат, когда закрылись оба.
func FanIn(a, b <-chan int) <-chan int {
	panic("TODO")
}
