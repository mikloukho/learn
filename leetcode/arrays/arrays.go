// Неделя 1: массивы и слайсы. Решай сверху вниз, они идут по возрастанию.
//
// Проверка: go test ./leetcode/arrays/
// Тесты уже написаны, сейчас они красные. Твоя задача — сделать зелёными.
package arrays

// --- 217. Contains Duplicate (Easy) ---
//
// Вернуть true, если в слайсе есть хотя бы один повтор.
//
// В лоб — два вложенных цикла, O(n^2). Так решать не надо.
// Идиома: мапа как множество, map[int]struct{} — struct{} занимает 0 байт.
func ContainsDuplicate(nums []int) bool {
	panic("TODO")
}

// --- 1. Two Sum (Easy) ---
//
// Найти индексы двух чисел, дающих в сумме target. Ровно одно решение,
// один элемент дважды не использовать. Порядок индексов не важен.
//
//	[2,7,11,15], target 9 -> [0,1]
//
// Ключ: за один проход клади в мапу "значение -> индекс" и на каждом шаге
// спрашивай, встречалось ли уже target-nums[i]. Один проход, O(n).
func TwoSum(nums []int, target int) []int {
	panic("TODO")
}

// --- 121. Best Time to Buy and Sell Stock (Easy) ---
//
// prices[i] — цена в день i. Купить один раз, продать позже. Максимальная
// прибыль, или 0 если заработать нельзя.
//
//	[7,1,5,3,6,4] -> 5   (купил по 1, продал по 6)
//
// Один проход: держи минимум слева и максимум разницы. Это приём
// "бегущий минимум", он вылезает в куче задач.
func MaxProfit(prices []int) int {
	panic("TODO")
}

// --- 283. Move Zeroes (Easy) ---
//
// Переместить все нули в конец, сохранив порядок остальных.
// МЕНЯТЬ НА МЕСТЕ, копию не создавать.
//
//	[0,1,0,3,12] -> [1,3,12,0,0]
//
// Приём "два указателя": медленный показывает, куда писать следующий
// ненулевой, быстрый бежит по слайсу.
//
// ВАЖНО для Go: слайс передаётся по значению, но указатель внутри — общий.
// Поэтому изменения элементов видны снаружи, а вот append здесь бы не сработал.
func MoveZeroes(nums []int) {
	panic("TODO")
}

// --- 26. Remove Duplicates from Sorted Array (Easy) ---
//
// Слайс ОТСОРТИРОВАН. Убрать дубли на месте, вернуть новую длину k.
// Первые k элементов должны быть уникальными, что дальше — не важно.
//
//	[0,0,1,1,1,2,2,3,3,4] -> k=5, nums = [0,1,2,3,4,...]
//
// Тоже два указателя. Раз отсортировано, дубли всегда стоят рядом.
func RemoveDuplicates(nums []int) int {
	panic("TODO")
}

// --- 88. Merge Sorted Array (Easy) ---
//
// nums1 длиной m+n: первые m — данные, дальше нули-заглушки.
// nums2 длиной n. Слить nums2 в nums1 НА МЕСТЕ, результат отсортирован.
//
//	nums1=[1,2,3,0,0,0] m=3, nums2=[2,5,6] n=3 -> [1,2,2,3,5,6]
//
// Ловушка: если идти слева направо, затрёшь ещё не обработанные элементы.
// Идти надо С КОНЦА — там гарантированно свободное место.
// Это тот же merge, что в Merge Two Sorted Lists, только на массивах.
func Merge(nums1 []int, m int, nums2 []int, n int) {
	panic("TODO")
}
