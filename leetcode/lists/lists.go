// Связные списки. По плану это неделя 3 — сначала массивы и строки.
//
// Проверка: go test ./leetcode/lists/
//
// ПОЧЕМУ ЭТА ТЕМА ТЯЖЕЛЕЕ ОСТАЛЬНЫХ
// Здесь нет ни одной подсказки в структуре данных: ни индексов, ни длины.
// Всё держится на указателях, а в PHP объекты всегда по ссылке и решать,
// где значение, а где адрес, было не нужно.
//
// ПРИЁМ, который решает половину задач темы — ФИКТИВНАЯ ГОЛОВА (dummy).
// Проблема без неё: первую ноду результата не к чему прицепить, и появляется
// особый случай. Заводишь пустую ноду, которую никто не увидит, и указатель
// tail на неё. Дальше в цикле всегда одно и то же: tail.Next = нода;
// tail = tail.Next. В конце возвращаешь dummy.Next.
package lists

type ListNode struct {
	Val  int
	Next *ListNode
}

// --- 21. Merge Two Sorted Lists (Easy) ---
//
// Слить два ОТСОРТИРОВАННЫХ списка в один отсортированный, переиспользуя
// существующие ноды (новые не создавать).
//
//	[1,2,4] + [1,3,4] -> [1,1,2,3,4,4]
//
// Итеративно: dummy + tail, цикл пока живы ОБА списка, берём меньшую голову,
// двигаем ТОТ список, откуда взяли, двигаем tail. После цикла остаток
// второго дописывается одним присваиванием — перебирать его не надо.
//
// Твоя первая попытка споткнулась на двух вещах: curr был типа int
// (сохранял значение вместо самой ноды) и list1/list2 не двигались,
// из-за чего for крутился вечно.
//
// Сложность: O(n+m) по времени, O(1) по памяти. Рекурсивный вариант тоже
// верен, но съедает O(n+m) стека — на собесе итеративный ценится выше.
func MergeTwoLists(list1 *ListNode, list2 *ListNode) *ListNode {
	panic("TODO")
}

// --- 206. Reverse Linked List (Easy) ---
//
// Развернуть список.  [1,2,3] -> [3,2,1]
//
// Три указателя: prev (сначала nil), curr, next. На каждом шаге запоминаешь
// next, разворачиваешь ссылку curr.Next = prev, сдвигаешь оба вперёд.
// Возвращаешь prev — он окажется на новой голове.
func ReverseList(head *ListNode) *ListNode {
	panic("TODO")
}

// --- 141. Linked List Cycle (Easy) ---
//
// Есть ли в списке цикл.
//
// Приём "черепаха и заяц" (алгоритм Флойда): медленный указатель идёт на 1,
// быстрый на 2. Есть цикл — они встретятся. Нет — быстрый упрётся в nil.
// Память O(1), в отличие от решения через мапу посещённых.
func HasCycle(head *ListNode) bool {
	panic("TODO")
}
