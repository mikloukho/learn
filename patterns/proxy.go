package patterns

// Proxy.
// Тот же интерфейс, что и у оригинала, но с доп. логикой
// (кеш, ленивая инициализация, контроль доступа) перед делегированием.
//
// TODO: интерфейс Subject, RealSubject, CachingProxy.
