package algorithms

// SelectionSort — сортировка выбором, O(n^2).
// Внимание: слайс передаётся по ссылке на массив — сортируем на месте.
//
// TODO: реализовать. Обмен: items[i], items[j] = items[j], items[i]
func SelectionSort(items []int) {
	panic("not implemented")
}
